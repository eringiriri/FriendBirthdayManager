<local:BaseWindow x:Class="FriendBirthdayManager.Views.ListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:FriendBirthdayManager.Views"
        xmlns:vm="clr-namespace:FriendBirthdayManager.ViewModels"
        mc:Ignorable="d"
        Title="{DynamicResource ListWindowTitle}"
        Height="600"
        Width="800"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- 検索エリア -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,15">
            <TextBlock Text="🔍" VerticalAlignment="Center" FontSize="18" Margin="0,0,5,0"/>
            <TextBox Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     Width="300"
                     VerticalContentAlignment="Center"
                     Margin="0,0,10,0">
                <TextBox.InputBindings>
                    <KeyBinding Key="Escape" Command="{Binding ClearSearchCommand}"/>
                </TextBox.InputBindings>
            </TextBox>
            <ComboBox SelectedIndex="{Binding SortIndex}" Width="120" Margin="0,0,10,0">
                <ComboBoxItem Content="{DynamicResource SortByNearest}"/>
                <ComboBoxItem Content="{DynamicResource SortByDate}"/>
                <ComboBoxItem Content="{DynamicResource SortByName}"/>
            </ComboBox>
            <Button Content="{DynamicResource ButtonImport}"
                    Command="{Binding ImportCsvCommand}"
                    Width="100"
                    Margin="0,0,10,0"/>
            <Button Content="{DynamicResource ButtonExport}"
                    Command="{Binding ExportCsvCommand}"
                    Width="100"/>
        </StackPanel>

        <!-- 友人リスト -->
        <DataGrid Grid.Row="1"
                  ItemsSource="{Binding Friends}"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserResizeRows="False"
                  GridLinesVisibility="Horizontal"
                  HeadersVisibility="Column"
                  AlternatingRowBackground="LightGray">
            <DataGrid.Columns>
                <DataGridTextColumn Header="{DynamicResource ColumnName}" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="{DynamicResource ColumnBirthday}" Binding="{Binding BirthdayDisplay}" Width="150"/>
                <DataGridTextColumn Header="{DynamicResource ColumnDaysUntil}" Binding="{Binding DaysUntilDisplay}" Width="80"/>
                <DataGridTextColumn Header="{DynamicResource ColumnNotification}" Binding="{Binding NotifyIcon}" Width="60"/>
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{DynamicResource ButtonEdit}"
                                    Command="{Binding DataContext.EditFriendCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="" Width="80">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="{DynamicResource ButtonDelete}"
                                    Command="{Binding DataContext.DeleteFriendCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                    CommandParameter="{Binding}"
                                    Background="#FFCCAA"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- ステータスバー -->
        <StatusBar Grid.Row="2" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</local:BaseWindow>
